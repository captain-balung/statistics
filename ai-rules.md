# ai-rules.md — AI 行為原則

## 預設許可清單

AI agent 在 Statistics Playground 專案中，**預設可以**做以下事情，不需逐項確認：

- 讀取本專案任何檔案
- 在 `/src`、`/lib`、`/content`、`/tests` 下建立或修改檔案
- 修改 `design.md`、`roadmap.md`、`log.md`
- 執行 `pnpm install`（已列於 package.json 的套件）
- 執行 `pnpm dev`、`pnpm build`、`pnpm test`、`pnpm lint`、`pnpm format`
- 執行 `git add`、`git commit`（本地）
- 查詢開發文件、MDN、React 文件、TypeScript 文件
- 修改本地分支（feature branch）
- 跑統計函式的單元測試
- 重構不改變對外行為的程式碼（但需更新對應測試）

**未列於本清單者一律視為「需確認才能做的事」。**

## 需確認才能做的事

以下操作每次都需要當下確認，**不接受「整個 session 一次授權」**：

- `git push`（任何分支）
- `git push --force`（任何情況）
- 合併到 `main` 或 `develop` branch
- 安裝新的 npm 套件（package.json 增加新 dependency）
- 升級主版本依賴（例：React 18 → 19）
- 修改 `spec.md`（包含新增/刪除功能、修改紅線、修改資安要點）
- 修改 `ai-rules.md`（本檔案）
- 修改 `README.md` 的主要功能段落
- 刪除任何已存在的測試
- 刪除 `/src`、`/lib`、`/content` 下的檔案（rename 視為刪除＋新增，亦需確認）
- 執行 `rm -rf` 或任何遞迴刪除
- 部署到任何環境（staging、production）
- 修改 `.env`、`.env.example`、`.gitignore`
- 新增需要 API key 或外部服務的功能（包含 analytics、AI 自動生成題目等）
- 修改 CI/CD 設定（`.github/workflows/`、`vercel.json` 等）
- 變更專案技術選型（更換框架、視覺化函式庫、動畫函式庫）

## 絕對不能做的事

以下是真的紅線，違反時 AI 應停下並回報，**不嘗試繞過**：

- 將 API key、密碼、token 寫死於程式碼中
- 將機密內容貼進對話或 log.md
- 跳過或停用測試以讓 CI 通過
- 修改 `log.md` 既有條目（只能 append，發現錯誤須新增「修正」類型條目）
- 未經確認執行不可逆操作（force push、刪除遠端分支、刪除生產資料等）
- 在 React 元件內直接實作統計運算（違反工程紅線第 1 條）
- 用 `any` 規避 TypeScript 型別檢查（違反工程紅線第 4 條）
- 為了「先動起來」而違反 `spec.md` 的工程紅線
- 收集學生個資或任何 PII（違反專案精神與 spec.md 的範圍邊界）
- 加入未經人類確認的第三方追蹤腳本（Google Analytics、Facebook Pixel 等）

## 不確定時的行為

採「**低估值並記錄到 log.md**」策略：

- 如果某操作介於「預設許可」與「需確認」之間，且影響範圍可逆（例如：修改一個元件的 props 介面），AI 可自行決定執行，但**必須**在 log.md 中以「AI 自主判斷」觸發來源記下。
- 如果某操作可能踩到「絕對不能做的事」附近（例如：不確定某個 import 是否會洩漏敏感路徑），**停下並問人類**，不要嘗試判斷。
- 如果不確定的是「會不會破壞既有功能」：先讀對應測試，若無測試則先補測試再動。
- 如果不確定某個統計概念的正確實作（例如：t 檢定的自由度公式選擇），**停下並問人類**，因為這直接影響教學內容的正確性。

對教學內容的正確性，AI 應採高於一般工程決策的謹慎標準——錯誤的數學會誤導學生。

## 回報義務

AI 主動回報的時機（**強制**，不可選擇性回報）：

1. **完成任務時**：摘要做了什麼、改了哪些檔案、是否更新了 roadmap.md 與 log.md
2. **卡住超過合理時間**：若同一問題反覆嘗試 3 次以上仍未解，停下回報，不要繼續猜測
3. **做了「AI 自主判斷」的決定**：明確告知人類「我自己決定了 X，理由是 Y，已記入 log.md」
4. **發現紅線可能被觸及**：例如發現原始程式碼中已有寫死的 token、或某個元件正在違反工程紅線——立即停下回報
5. **偵測到敏感資料**：在處理任何資料時若意外發現疑似個資、密鑰、私人資訊，立即停下回報，不繼續處理

### 對本專案特有的額外回報義務

6. **發現統計函式的疑似錯誤**：例如測試通過但結果與標準教科書不符，必須停下回報並附上對照來源（教科書頁碼、權威統計軟體輸出）。
7. **發現教學內容（解釋區、誤解提醒）可能有誤**：教學錯誤的成本對學生很高，發現疑慮即停下，不擅自修改。

## 跨專案複用建議

本檔案內容大部分跨小型專案可複用。下次新開專案時：

1. 直接複製本檔案到新專案
2. 移除「對本專案特有的額外回報義務」與「絕對不能做的事」中與統計教學相關的條目
3. 依新專案的領域加入特定紅線（例如：金融類專案需加入「不可在日誌中印出完整卡號」等）
