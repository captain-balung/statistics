# spec.md — Statistics Playground 規格書

## A. 總則區

### 專案精神

我們追求「**讓學生看見統計**」，**不是**「讓學生用網頁算統計」。

本專案存在的理由是：傳統統計教學讓學生被公式淹沒、把統計當成代數題目背誦，最終會算但不懂。本網站的目的是讓抽象概念變成可拖、可拉、可模擬的物件，讓學生在「玩」的過程中建立直覺。

具體對照：

- 我們做的是「統計遊樂場」，**不做**「網頁版 SPSS」。功能完整度不是我們的指標，操作後是否產生「啊原來如此」的洞察才是。
- 我們追求「概念可視化」，**不追求**「計算精準度的最後一位小數」。當兩者衝突時，優先讓動畫流暢與概念清晰。
- 我們追求「即時回饋」，**不接受**「按下按鈕等三秒看結果」。

這段精神可被引用拒絕方案，例如：「這個功能會跳出新分頁顯示結果，違反『即時回饋』精神」。

### 總體規範

1. **視覺化先於數值**：每個統計量都必須有對應的視覺呈現。純數字輸出（不附圖）視為違反本條。
2. **參數變動必須即時反應**：滑桿拖動時，圖表與數值必須在同一個 frame 更新（< 16ms 的計算除外可降頻）。延遲顯示或需手動觸發計算視為違反本條。
3. **每個概念至少有一種「動手」操作**：純閱讀說明的模組不被接受。學生必須能拖、能拉、能按。
4. **錯誤觀念主動提醒**：右側解釋區必須包含「學生常見誤解」段落，而不是只有教科書定義。
5. **統計函式集中於 `/lib/statistics`**：任何模組元件不得自行實作平均數、標準差、t 檢定等基礎運算。違反此條者，code review 時應退件。

### 跨章節一致性原則

- 同一個統計量在不同模組中，視覺呈現方式應保持一致（例如：標準差永遠用同色系陰影帶表示）。
- 同一個概念若在多模組出現，文字定義應一致（由 `/content/glossary` 集中管理）。

### 視覺設計原則

本網站的視覺氣質是**「科普雜誌的編輯設計」**，不是 SaaS 後台、不是 edtech 模板、不是兒童遊戲。

具體對照（每條都可用於 review 時拒絕方案）：

1. **編輯感優於工具感**：版面參考點是 *Quanta Magazine*、*Nautilus*、*Pudding* 這類資料新聞網站，**不是** Notion、Linear、Khan Academy。具體後果：標題用 serif、留白慷慨、圖表可以「破版」（橫跨多欄）、不怕一頁只有一張圖一段字。
2. **飽和原色，不用漸層**：以高飽和的少數原色（紅、藍、黃為核心）做重點，**禁用**紫粉漸層、彩虹漸層、毛玻璃漸層等「AI 預設美學」配色。背景與內文使用米白與墨黑，避免純白純黑。
3. **數字是主角**：數值顯示使用等寬（monospaced）字體確保對齊；數字字級必須夠大，學生不需要瞇眼。任何「滑桿旁邊的當前值」字級不得小於 16px。
4. **動畫服務認知，不裝飾**：每個動畫必須回答「這個動畫讓學生理解了什麼？」。**禁用**純裝飾的 hover 彈跳、進場淡入、滑鼠軌跡特效。允許的動畫類別只有：(a) 參數變動的圖表過渡、(b) 模擬過程的逐步呈現、(c) 操作回饋（按下、拖曳中、釋放）。
5. **不要怕嚴肅**：學生不是小學生。**禁用**卡通插畫、emoji 大量裝飾（功能性的 ✅⛔ 例外）、圓角過大（≥ 16px）的「友善風」按鈕。圖示風格採線稿（line icons），不採填色卡通。
6. **手寫感保留給「概念解釋區」**：圖表本體必須精準（直線是直線、刻度是刻度），但右側解釋區可使用輕微的手寫感字體於關鍵詞，呼應「老師在白板上圈起來」的感覺。這是本網站唯一的「玩心」出口。
7. **深色模式為次要支援**：第一版以淺色為主。深色模式可有可無，**不可**為了深色模式犧牲淺色模式的設計選擇。

具體 token（色票、字級、間距）由 `design.md` 的「設計系統」一節定義。本節是原則，那節是規格。

---

## B. 產品定義區

### 核心命題

一個互動式統計學教學網站，讓學生透過拖動參數、執行模擬、觀察動畫，建立統計概念的直覺，作為傳統公式教學的補充教材。

### 目標使用者

- **主要使用者**：台灣的高中（含學測準備期）與大學一二年級學生
- **次要使用者**：統計學教師（用於課堂展示）
- **技術背景**：能操作網頁，不假設會寫程式
- **裝置**：以筆電/桌機瀏覽器為主，平板可用，手機為次要支援
- **使用情境**：
  - 課後自學
  - 教師上課即時展示
  - 寫作業前的概念釐清

### 範圍邊界

**做什麼：**

- 7 大模組的互動式視覺化（見功能清單）
- 中文界面、繁體中文教學內容
- 每個模組的「探索／引導／挑戰／模擬」四模式
- 統計函式庫（`/lib/statistics`）支援前述模組所需運算

**明確不做什麼：**

- **不做**使用者帳號系統、登入、學習進度雲端儲存（最多用 localStorage 記偏好）
- **不做**作業批改、成績計算、班級管理功能
- **不做**資料匯入（學生不能上傳自己的 CSV）。所有資料都在頁面內互動產生
- **不做**多語系。第一版只有繁體中文
- **不做**統計檢定的完整列表（卡方、ANOVA、無母數方法第一版均不做）
- **不做**手機優先設計。手機可看但不保證所有互動順暢
- **不做**離線版／PWA
- **不做**內建社群、評論、學生作答分享
- **不做**付費功能、訂閱制

### 成功標準

1. **可驗收項 A**：學生在「常態分配中央極限定理」模組中，能在 3 分鐘內透過操作達成「從非常態母體抽樣後，樣本平均仍呈常態」的觀察結論（透過內建挑戰模式自動判定）。
2. **可驗收項 B**：所有 7 大模組的核心互動（拖滑桿、點按鈕）在 Chrome 與 Safari 最新版的反應時間 < 100ms（從滑桿釋放到畫面更新）。
3. **可驗收項 C**：每個模組的右側概念解釋區都包含「學生常見誤解」段落，且至少列出 2 條具體誤解。

---

## C. 功能與資安區

### 工程紅線

1. **禁止在 React 元件內直接實作統計運算**：所有統計函式必須走 `/lib/statistics`，違者退件。
2. **禁止在生產環境留下 `console.log`**：開發用日誌需走 logger 包裝，build 時剝除。
3. **禁止 catch 後吞例外不處理**：catch 必須至少做一件事（記錄、轉拋、UI 顯示），不可空 catch。
4. **禁止用 `any` 規避型別檢查**：TypeScript `strict` 模式為強制要求，無法解決時應改用 `unknown` + type guard。
5. **禁止單一檔案超過 500 行**：超過時必須拆分。模組元件應依「視覺化/控制區/解釋區」自然切割。

### 功能清單

| ID | 模組 | 功能 | 驗收條件 |
|---|---|---|---|
| F1-1 | 敘述統計量 | 資料點拖曳實驗室 | 拖曳/新增/刪除資料點；平均數、中位數、標準差三項數值即時更新；拖動延遲 < 100ms |
| F1-2 | 敘述統計量 | 平均數平衡木動畫 | 槓桿視覺化呈現；新增資料點時槓桿傾斜動畫播放；平均數位置為支點且自動置中 |
| F1-3 | 敘述統計量 | 標準差伸縮動畫 | 平均數固定下，調整標準差滑桿時分布圖形即時伸縮；附對照（兩組同平均不同標準差） |
| F2-1 | 相關與回歸 | 散佈圖生成器 | 相關係數滑桿可設定 -1 ~ +1；散佈圖即時生成；Pearson r、回歸線、R² 同步顯示 |
| F2-2 | 相關與回歸 | 人工製造相關係數 | 學生可拖曳任一散點；Pearson r 即時重算且顯示誤差 < 1e-6 |
| F2-3 | 相關與回歸 | 回歸線誤差動畫 | 每點到回歸線的垂直距離可視化；SSE 計算過程動畫化（平方擴展視覺） |
| F3-1 | 機率 | 賭博遊戲模擬器 | 至少支援骰子、撲克、輪盤、老虎機四種；顯示中獎率、累積輸贏、長期期望值 |
| F3-2 | 機率 | 二項式分配實驗室 | 可調 n、p；長條圖呈現各成功次數機率；參數變動即時重繪 |
| F3-3 | 機率 | 大數法則模擬器 | 連續擲硬幣動畫；正面比例變化曲線即時繪製；可調擲幣速度 |
| F4-1 | 常態分配 | 常態曲線互動工具 | μ、σ 滑桿；曲線即時變形；可顯示多條曲線疊圖 |
| F4-2 | 常態分配 | 面積與機率動畫 | 拖曳區間端點；陰影面積即時計算並顯示對應機率（誤差 < 1e-4） |
| F4-3 | 常態分配 | 中央極限定理模擬器 | 母體可選均勻/骰子/偏態；連續抽樣動畫；樣本平均分布逐步成形 |
| F4-4 | 常態分配 | Z/PR 雙向換算 | 模式 A：輸入 Z 顯示 PR + 曲線位置；模式 B：輸入 PR 顯示 Z + 曲線切點 |
| F5-1 | 母體與抽樣分布 | 抽樣分布模擬器 | 母體可選常態/均勻/偏態/雙峰/自訂；可調 n；單次抽樣動畫；抽樣分布逐步成形；n 變大時抽樣分布變窄 |
| F6-1 | T 分配 | 從母體生成 T 分配 | 可調母體分布、n、是否已知 σ；每次抽樣計算 t 並丟入分布；t 與常態疊圖 |
| F7-1 | 假設檢定 | 總體動畫 | H₀、H₁、臨界區、p-value 四要素以動畫呈現 |
| F7-2 | 假設檢定 | 符號檢定 | 輸入前測後測；只顯示正負號；↑↓ 視覺化 |
| F7-3 | 假設檢定 | Z 檢定 | 反覆抽樣動畫；顯示樣本平均、抽樣分布、Z 分數在分布中的位置 |
| F7-4a | 假設檢定 | 單一樣本 T 檢定 | 母體平均、樣本平均、t 分布三者同畫面呈現 |
| F7-4b | 假設檢定 | 獨立樣本 T 檢定 | 兩組分布同時顯示；平均差視覺化；t 值變化即時顯示 |
| F7-4c | 假設檢定 | 成對樣本 T 檢定 | before/after 用線連接的視覺化；差值分布顯示 |
| FX-1 | 跨模組 | 四種學習模式 | 每個模組皆支援探索／引導／挑戰／模擬四模式切換 |
| FX-2 | 跨模組 | 三區布局 | 每頁皆為「左控制／中視覺／右解釋」三欄式布局 |

### 反例規格

明文記下「這軟體不該做什麼」：

- 模擬器**不該**在學生未按開始前自動跑（會嚇到使用者）
- 統計函式**不該**在無效輸入時 throw 給 UI 看（應回傳 `{ ok: false, reason }`）
- 數值顯示**不該**有「12.347293847...」這種沒截斷的小數（一律保留至有意義的位數）
- 動畫**不該**沒有「暫停／重置」按鈕（學生可能想停在某一刻觀察）
- 概念解釋**不該**直接搬教科書定義（要白話文，要點出常見誤解）
- 圖表座標軸**不該**自動縮放到讓重點看不出來（例如 CLT 模組中如自動縮放會看不出分布收窄）
- 挑戰模式**不該**只說「答錯」不解釋（必須附上錯在哪、正確思路）

### 資料與資安要點

本專案為純前端教學網站，資料敏感度低，但仍有三條最關鍵的：

1. **處理什麼資料**：學生在頁面內產生的資料點（座標、滑桿值）、可能存於 localStorage 的偏好設定（如選擇的學習模式）。**不處理**任何個資、登入資訊、上傳檔案。
2. **資料敏感度**：低。最敏感的可能是 localStorage 中的學習偏好，不視為 PII。**第一版（MVP）不使用任何第三方 analytics 或追蹤腳本**（見 `kickoff-decisions.md` D-002）。若未來新增 analytics（如 Plausible），需經人類確認並更新本欄位。
3. **AI 不可接觸什麼資產**：本專案目前無外部 API key、無資料庫、無使用者個資。若未來新增（例如 OpenAI API 用於自動產生練習題），AI 必須使用環境變數，不可寫死於程式碼，且 `.env` 必須加入 `.gitignore`。
